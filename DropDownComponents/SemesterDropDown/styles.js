import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    semester_dropdown: {
        width: 200,
        left: "20%",
        top: "5%",
      },
});

export default styles;
